import 'package:flutter_test/flutter_test.dart';
import 'package:flutterduplo/flutterduplo.dart';
// import 'package:flutterduplo/flutterduplo_platform_interface.dart';
// import 'package:flutterduplo/flutterduplo_method_channel.dart';

// class MockFlutterduploPlatform
//     with MockPlatformInterfaceMixin
//     implements FlutterduploPlatform {
//
//   @override
//   Future<String?> getPlatformVersion() => Future.value('42');
// }

void main() {
  // final FlutterduploPlatform initialPlatform = FlutterduploPlatform.instance;
  //
  // test('$MethodChannelFlutterduplo is the default instance', () {
  //   expect(initialPlatform, isInstanceOf<MethodChannelFlutterduplo>());
  // });

  test('getPlatformVersion', () async {
    // Flutterduplo flutterduploPlugin = Flutterduplo();
    // MockFlutterduploPlatform fakePlatform = MockFlutterduploPlatform();
    // FlutterduploPlatform.instance = fakePlatform;
    //
    // expect(await flutterduploPlugin.getPlatformVersion(), '42');
  });
}
